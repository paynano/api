{
  "name": "pursekeeper-api",
  "version": "0.2.0",
  "description": "Pay-per-call HTTP API paid in Nano: X-Nano-Payment bearer hash or x402 exact on nano:mainnet",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "license": "MIT",
  "repository": { "type": "git", "url": "git+https://github.com/pursekeeper/api.git" },
  "engines": { "node": ">=22" },
  "dependencies": {
    "@x402/core": "^2.25.0",
    "@x402nano/exact": "^0.2.1",
    "@x402nano/helper": "^0.1.1",
    "nanocurrency": "^2.5.0"
  }
}
